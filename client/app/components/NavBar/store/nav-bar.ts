export interface UserProfileState {
  userName: string,
  firstName: string,
  lastName: string,
  gender: boolean,
  userId: number,
  isAuthenticated: boolean,
  isReady: boolean,
  appTitle: string
}
